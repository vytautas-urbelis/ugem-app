import { Platform } from "react-native";

export const GOOGLE_ADDRESS_API = Platform.select({
  ios: ``,
  android: ``,
});

export const iosClientId = "";
export const webClientId = "";

export const revenCatAppleAPI = "";
export const revenCatAndroidAPI = "";
